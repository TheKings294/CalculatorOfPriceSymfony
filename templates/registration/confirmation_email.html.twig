{% extends 'base.html.twig' %}

{% block title %}Confirm your email | YourSaaS{% endblock %}

{% block body %}
    <div class="min-h-screen bg-gray-50 flex items-center justify-center px-6">

        <div class="w-full max-w-md bg-white rounded-2xl shadow-xl p-8 text-center space-y-6">

            {# Icon / visual cue #}
            <div class="flex justify-center">
                <div class="flex h-12 w-12 items-center justify-center rounded-full bg-blue-100">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-blue-600" fill="none"
                         viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                              d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8m-18 8h18" />
                    </svg>
                </div>
            </div>

            {# Title #}
            <h1 class="text-2xl font-extrabold text-gray-900">
                Confirm your email address
            </h1>

            {# Description #}
            <p class="text-gray-600 text-sm">
                Thanks for signing up! Please confirm your email address to activate your account.
            </p>

            {# CTA #}
            <a href="{{ signedUrl|raw }}"
               class="inline-flex items-center justify-center w-full rounded-xl bg-blue-600 py-3 text-white font-semibold
                  hover:bg-blue-700 transition focus:outline-none focus:ring-2
                  focus:ring-blue-500 focus:ring-offset-2">
                Confirm my email
            </a>

            {# Expiration info #}
            <p class="text-xs text-gray-500">
                This link will expire in
                <span class="font-medium">
                {{ expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle') }}
            </span>.
            </p>

            {# Footer text #}
            <p class="text-sm text-gray-700 pt-2">
                If you didn’t create an account, you can safely ignore this email.
            </p>

        </div>
    </div>
{% endblock %}
