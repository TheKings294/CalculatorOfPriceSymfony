/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        './templates/**/*.twig',
        './assets/**/*.js',
    ],
    darkMode: 'class', // enable dark mode via class
    theme: {
        extend: {},
    },
    plugins: [],
}
